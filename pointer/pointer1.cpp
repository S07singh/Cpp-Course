#include<iostream>
using namespace std;

int main(){

    // What  is a pointer? ---> data type which holds the address of otherdata types

    int a=3;
    int* b = &a;

    // & ---> Address of operator 
    
    // * ---> Dereference of operator


    return 0;
}